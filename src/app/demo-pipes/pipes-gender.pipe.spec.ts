import { PipesGenderPipe } from './pipes-gender.pipe';

describe('PipesGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new PipesGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
